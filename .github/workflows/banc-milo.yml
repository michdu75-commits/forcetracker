name: Banc d'essai de Milo (référence comportementale)

# ══════════════════════════════════════════════════════════════════════════════════════
# POURQUOI CE WORKFLOW EXISTE — mesuré le 20/09/2026.
#
# La seule référence réelle du comportement de Milo vivait dans le `localStorage` d'un
# téléphone : le dépôt ne portait qu'un rapport « à blanc » (0 appel), donc le jour d'un
# changement de contexte, de prompt ou de moteur, il n'y avait RIEN à comparer.
#
# ⛔⛔ ET LE CONTENEUR DE DÉVELOPPEMENT NE PEUT PAS LA PRODUIRE. Mesuré, cause précise :
# le proxy de sortie répond « 403 à CONNECT (refus de politique) » pour
# michdu75-commits.github.io, dry-field-e931.forcetracker-app.workers.dev ET
# script.google.com. Le banc pilote l'app EN LIGNE, qui appelle le Worker, qui porte la
# clé API — les trois maillons sont hors d'atteinte. Un runner GitHub, lui, a le réseau.
#
# ⚠️⚠️ IL NE SE DÉCLENCHE JAMAIS TOUT SEUL, ET C'EST LA DÉCISION CENTRALE.
# Pas de `push`, pas de `schedule` : **uniquement `workflow_dispatch`**, avec une
# confirmation à taper. Une passe = 57 appels IA RÉELS, facturés. Un workflow qui part
# sur chaque push brûlerait le plafond quotidien et la facture sans que personne
# ne l'ait demandé.
#
# ⚖️ CE QU'IL NE FAIT PAS : il ne juge pas, il n'introduit aucun juge IA, et il ne rend
# pas le banc bloquant. Il MESURE et il ENREGISTRE. *Le banc reste le niveau 3 —
# définition → comportements observables → tests — il ne devient pas la définition.*
#
# 🔒 DONNÉES : le rapport ne contient QUE des identifiants de scénario, des verdicts et
# des nombres. Vérifié le 20/09 — aucune clé `reply`, `reponse` ni `content`. Les
# scénarios emploient des profils synthétiques posés par leurs propres fixtures.
# ══════════════════════════════════════════════════════════════════════════════════════

on:
  workflow_dispatch:
    inputs:
      confirmer:
        description: 'Tape LANCER pour confirmer — cette passe envoie de VRAIS appels IA, facturés.'
        required: true
        default: ''
      scenarios:
        description: 'Nombre de scénarios (vide = tous). Ex. 5 pour un essai bon marché.'
        required: false
        default: ''
      enregistrer:
        description: 'Committer la référence dans le dépôt ? (oui / non)'
        required: false
        default: 'oui'
      banc:
        description: 'Quel banc ? ev = le banc complet (défaut) · r34 = le petit banc R34 « niveau d activité » (7 appels prévus, 14 au plus, jamais enregistré).'
        required: false
        default: 'ev'

permissions:
  contents: write

concurrency:
  # ⛔ Deux passes en parallèle brûleraient le plafond et se couperaient l'une l'autre.
  group: banc-milo
  cancel-in-progress: false

jobs:
  banc:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Refuser si la confirmation n'est pas tapée
        # ⭐ Une confirmation explicite, pas une case cochée par défaut : ce workflow
        #    dépense de l'argent réel. On préfère refuser que deviner.
        run: |
          if [ "${{ github.event.inputs.confirmer }}" != "LANCER" ]; then
            echo "::error::Confirmation absente. Relance en tapant LANCER dans le champ."
            exit 1
          fi

      - name: R34 — refuser si on demande d'enregistrer
        # ⛔ Le banc R34 (25/09/2026) est une MESURE ponctuelle avant publication : il ne
        #    produit pas de référence et ne committe rien. Refusé AVANT toute dépense si
        #    quelqu'un le lance avec « enregistrer » à autre chose que non.
        if: ${{ github.event.inputs.banc == 'r34' && github.event.inputs.enregistrer != 'non' }}
        run: |
          echo "::error::Le banc r34 ne s'enregistre jamais. Relance avec enregistrer = non."
          exit 1

      - name: Refuser TOT si l'identite du banc manque
        # ⛔⛔ AVANT LE CHECKOUT, ET SURTOUT AVANT TOUTE DEPENSE. Sans jeton S1, chaque
        #    scenario part, se fait refuser en 401 et revient vide : la passe coute son
        #    temps et n'apprend rien. Mesure du 20/09 : c'est exactement ce qui est arrive.
        # ⛔ ON NE MONTRE JAMAIS LA VALEUR — ni en clair, ni tronquee, ni sa longueur exacte
        #    si elle est correcte. On dit seulement PRESENT / ABSENT et QUOI configurer.
        #    (GitHub masque deja les secrets dans les journaux ; on ne s'y fie pas seul.)
        env:
          FT_BANC_TOKEN: ${{ secrets.FT_BANC_TOKEN }}
        run: |
          if [ -z "$FT_BANC_TOKEN" ]; then
            echo "::error::Le secret FT_BANC_TOKEN n'est pas defini. Le banc ne peut pas s'identifier aupres de Milo."
            echo "::error::A faire UNE fois : dans l'app -> Profil -> Admin -> « Jeton du banc d'essai » -> Fabriquer -> Copier."
            echo "::error::Puis GitHub -> Settings -> Secrets and variables -> Actions -> New repository secret."
            echo "::error::Nom exact du secret : FT_BANC_TOKEN"
            echo "::error::Ne copie JAMAIS ce jeton depuis un journal : il n'y apparait jamais."
            exit 1
          fi
          echo "::notice::identite du banc : presente"

      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          # ⚠️ ALIGNÉ SUR LE CONTENEUR DE DÉVELOPPEMENT (v22), où les 57 scénarios ont été
          #    éprouvés. Ce n'était PAS la cause de l'échec du 20/09 — celle-ci était une
          #    dépendance non déclarée, et `MODULE_NOT_FOUND` tombe sur n'importe quelle
          #    version de Node. On aligne parce qu'un banc jugé sur un autre moteur que
          #    celui qui l'a validé est une divergence de la même famille, pas parce que
          #    cela « répare » quoi que ce soit.
          # ⛔ Et cela NE FAIT PAS TAIRE l'avertissement « Node.js 20 is deprecated » de
          #    GitHub : celui-là parle du moteur interne des actions (checkout, setup-node,
          #    upload-artifact), pas du nôtre. Il est sans rapport avec cet échec.
          node-version: '22'

      - name: Installer les dépendances (Playwright est DÉCLARÉ dans package.json)
        # ⛔⛔ C'EST ICI QUE L'ÉCHEC DU 20/09 SE JOUAIT. L'ancienne ligne était
        #    `npx --yes playwright@1.47.0 install --with-deps chromium` : elle télécharge
        #    les NAVIGATEURS, mais n'installe PAS le paquet dans le dépôt. `require(...)`
        #    n'avait donc rien à trouver. Et elle épinglait 1.47.0 alors que les scénarios
        #    sont éprouvés sur 1.56.1 — deux versions différentes pour un même banc.
        # ⭐ `npm ci` installe EXACTEMENT ce que dit package-lock.json : même version ici
        #    et en développement, sans qu'on ait à le répéter à deux endroits (R2).
        run: npm ci --no-audit --no-fund

      - name: Installer le navigateur sans tête
        # ⭐ Sans version en dur : c'est le Playwright qu'on vient d'installer qui télécharge
        #    le navigateur qui lui correspond. Une version écrite ici redeviendrait fausse au
        #    premier changement de package.json, en silence.
        run: npx playwright install --with-deps chromium

      - name: ⭐ CONTRÔLE GRATUIT — l'infrastructure marche-t-elle, SANS dépenser un appel ?
        # ⛔⛔ CETTE ÉTAPE EXISTE POUR NE PLUS PAYER UN ÉCHEC D'ENVIRONNEMENT. Le 20/09, le
        #    workflow est mort en 34 s sur un défaut qui n'avait RIEN à voir avec Milo — mais
        #    rien ne distinguait ce cas d'un vrai problème de banc.
        # ⭐ Le run À BLANC est gratuit PAR CONSTRUCTION : il ne passe aucun appel (c'est
        #    `--go` qui les déclenche). Il prouve pourtant toute la chaîne — Node démarre,
        #    eval.js charge, Playwright est résolu, le navigateur se lance, l'app est servie,
        #    et le contexte réel est construit jusqu'à l'étape qui PRÉCÈDE les appels.
        # 👉 *Si ça casse ici, ça n'a rien coûté ; si ça passe ici, ce qui casse ensuite est
        #    un vrai sujet de banc.*
        run: |
          set -o pipefail
          node tests/milo/eval.js --n 3 2>&1 | tee /tmp/blanc.log
          node -e "
            const r = require('./tests/milo/eval-report.json');
            if (r.mode !== 'blanc') { console.error('ECHEC : le controle gratuit a fait des appels (mode ' + r.mode + ')'); process.exit(1); }
            const pw = require('./tests/_playwright.js').chargerPlaywright();
            if (pw.voie !== 'node_modules') { console.error('ECHEC : Playwright resolu par ' + pw.voie + ' — la dependance declaree n a pas servi'); process.exit(1); }
            console.log('OK : Playwright resolu depuis ' + pw.chemin);
          "
          grep -q 'DEVIS MESURÉ' /tmp/blanc.log || { echo '::error::le contexte reel n a pas ete construit'; exit 1; }
          echo '::notice::infrastructure verte, zero appel IA depense'

      - name: Lancer la passe RÉELLE
        if: ${{ github.event.inputs.banc != 'r34' }}
        id: passe
        # ⭐ LE JETON N'ARRIVE QU'ICI. Le controle gratuit juste au-dessus n'appelle personne :
        #    lui donner l'identite ne prouverait rien et l'exposerait pour rien.
        #    ⛔ `eval.js` ne l'affiche jamais : il en verifie la PRESENCE et la longueur, puis
        #    le pose dans le `localStorage` du navigateur de test, la ou le vrai client le
        #    range. Le banc emprunte donc le chemin de tout le monde.
        env:
          FT_BANC_TOKEN: ${{ secrets.FT_BANC_TOKEN }}
        run: |
          set -o pipefail
          ARGS="--go"
          if [ -n "${{ github.event.inputs.scenarios }}" ]; then
            ARGS="$ARGS --n ${{ github.event.inputs.scenarios }}"
          fi
          echo "::notice::node tests/milo/eval.js $ARGS"
          node tests/milo/eval.js $ARGS 2>&1 | tee /tmp/banc.log
          echo "rc=$?" >> "$GITHUB_OUTPUT"

      - name: Vérifier que la passe a VRAIMENT tourné
        if: ${{ github.event.inputs.banc != 'r34' }}
        # ⛔⛔ UNE PASSE TRONQUÉE NE DOIT JAMAIS PASSER POUR UNE RÉFÉRENCE. C'est la leçon
        #    de `BUGS.md` §61 (« un run planté ressemble à un run vert ») : on relit le
        #    rapport produit et on exige qu'il annonce le mode RÉEL. Un rapport « blanc »
        #    signifie que `--go` n'a pas pris — il ne vaut rien comme référence.
        run: |
          node -e "
            const r = require('./tests/milo/eval-report.json');
            if (r.mode === 'blanc') { console.error('ECHEC : le rapport est en mode blanc, aucun appel n a ete fait.'); process.exit(1); }
            if (!r.sha) { console.error('ECHEC : le rapport ne porte pas de SHA — il ne peut pas servir de reference.'); process.exit(1); }
            const p = (r.parPasse && r.parPasse.prod) || [];
            const joues = p.filter(x => x.etat !== 'blanc').length;
            if (joues < 1) { console.error('ECHEC : aucun scenario joue.'); process.exit(1); }
            // ⛔⛔ « JOUE » NE VEUT PAS DIRE « A REPONDU ». Mesure du 20/09 : un essai a
            //    1 scenario est revenu HTTP 401, donc zero comportement — et ce controle
            //    disait OK. Un scenario sans reponse est compte comme joue.
            const repondu = p.filter(x => x.etat === 'vert' || x.etat === 'rouge').length;
            const muets = p.filter(x => x.etat === 'muet' || x.etat === 'erreur').length;
            if (repondu < 1) {
              console.error('ECHEC : ' + joues + ' scenario(s) joue(s) mais AUCUNE reponse ('
                + muets + ' muet(s)/erreur(s)). La passe est bien formee et VIDE.');
              console.error('Cause la plus frequente : le Worker refuse (401) — le navigateur');
              console.error('du banc n a aucun jeton d identite (« Origin correct + aucun token -> refus »).');
              process.exit(1);
            }
            console.log('OK : ' + repondu + ' reponse(s) sur ' + joues + ' joue(s), mode ' + r.mode + ', sha ' + r.sha + ', version ' + (r.version||'?'));
          "

      - name: R34 — arbre AVANT (le code d'avant B1/D-021, sur cette même branche)
        # ⭐ f4f9317d = le dernier commit de la branche AVANT le chantier B1/B2/D-021 : il
        #    porte TOUT le reste du code non publié, donc la seule différence mesurée est
        #    celle du chantier. Extrait dans /tmp, le checkout n'est jamais déplacé.
        if: ${{ github.event.inputs.banc == 'r34' }}
        env:
          R34_AVANT_SHA: f4f9317de8eea7af81f2dcb492c9a728f76f3c82
        run: |
          git fetch --no-tags --depth=1 origin "$R34_AVANT_SHA"
          test "$(git rev-parse FETCH_HEAD)" = "$R34_AVANT_SHA"
          mkdir -p /tmp/avant && git archive FETCH_HEAD | tar -x -C /tmp/avant
          test -f /tmp/avant/index.html && echo "::notice::arbre AVANT $R34_AVANT_SHA extrait"

      - name: R34 — devis À BLANC (0 appel, gratuit)
        # ⭐ Le devis se refait ICI, sur le runner, avant la passe réelle : contexte réel
        #    mesuré, preuve de couverture de l'arbre AVANT, Worker bloqué, pas de jeton.
        if: ${{ github.event.inputs.banc == 'r34' }}
        env:
          # ⭐ Pour r34, « scenarios » = la liste des cas (ex. R34-A,R34-B). Passée par l'environnement,
          #    JAMAIS interpolée dans le shell ; le script la refuse si elle n'est pas une liste d'identifiants connus.
          R34_CAS: ${{ github.event.inputs.scenarios }}
        run: |
          set -o pipefail
          node tests/milo/r34_banc.js --avant /tmp/avant 2>&1 | tee /tmp/r34-blanc.log

      - name: R34 — passe RÉELLE (plafonds 20 appels et 5 €, vérifiés par le script)
        if: ${{ github.event.inputs.banc == 'r34' }}
        env:
          FT_BANC_TOKEN: ${{ secrets.FT_BANC_TOKEN }}
          R34_AVANT_SHA: f4f9317de8eea7af81f2dcb492c9a728f76f3c82
          R34_CAS: ${{ github.event.inputs.scenarios }}
        run: |
          set -o pipefail
          node tests/milo/r34_banc.js --go --avant /tmp/avant 2>&1 | tee /tmp/r34.log

      - name: R34 — garder le journal et le rapport en pièce jointe
        if: ${{ always() && github.event.inputs.banc == 'r34' }}
        uses: actions/upload-artifact@v4
        with:
          name: banc-r34-journal
          path: |
            /tmp/r34-blanc.log
            /tmp/r34.log
            tests/milo/r34-report.json
          retention-days: 30

      - name: Enregistrer la référence dans le dépôt
        if: ${{ github.event.inputs.enregistrer != 'non' }}
        run: |
          python3 tools/gen_banc_reference.py
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add tests/milo/eval-report.json tests/milo/eval-report.md docs/BANC-MILO-REFERENCE.md
          if git diff --cached --quiet; then
            echo "::notice::rien à enregistrer (référence identique)"
          else
            git commit -m "banc de Milo : reference comportementale reelle (passe du $(date -u +%F))

          Passe REELLE lancee a la main depuis l'onglet Actions. Ce fichier est le point
          de comparaison exige par R34 avant tout changement de contexte, de prompt ou
          de moteur : voici ce que Milo faisait AVANT.

          Aucune conversation enregistree : identifiants de scenario, verdicts et nombres.

          Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
            git push
          fi

      - name: Garder le journal complet en pièce jointe
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: banc-milo-journal
          path: |
            /tmp/banc.log
            tests/milo/eval-report.json
            tests/milo/eval-report.md
          retention-days: 30
